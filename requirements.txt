# AICarRacing — 실행에 필요한 패키지 (팀 B)
# 테스트 환경: python 3.11. 학습=A100 CUDA(conda teamB_env), 평가/녹화=macOS CPU(conda racing).
# 설치:  pip install -r requirements.txt
# (기존 실습 docker image에 pytorch가 이미 있으면 아래 torch 줄은 건너뛰어도 됨)

# --- 핵심 (필수) ---
torch>=2.6            # 테스트: 2.12.0(CPU) / CUDA 빌드(A100). 체크포인트가 torch.load(weights_only=False)를 쓰므로 2.6+ 필요
gymnasium==1.3.0      # CarRacing-v3 환경
box2d-py==2.3.8       # Box2D 물리 (gymnasium[box2d] 로도 설치됨). 일부 환경은 빌드 시 swig 필요
pygame==2.6.1         # CarRacing 렌더링 백엔드
opencv-python         # ★ 필수: src/env_wrappers.py 의 cv2 (grayscale 변환). 없으면 모든 스크립트가 import 단계에서 실패
numpy==2.4.6

# --- 로깅 / 그림 ---
tensorboard==2.20.0   # 학습 스칼라 로깅
matplotlib==3.10.9    # 평가 플롯 / 보고서 figure

# --- 영상 녹화 (선택: record_video.py 사용 시) ---
imageio==2.37.3
imageio-ffmpeg==0.6.0
